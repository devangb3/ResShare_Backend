# ResShare_Backend

## C++
`sudo apt install openssl`

`sudo apt install libssl-dev`

## Python
Version 3.8

`pip install pybind11`

`pip install request`

`pip install os`

## Build
`cd bazel`

`bazel build //...`
